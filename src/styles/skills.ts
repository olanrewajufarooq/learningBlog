import clsx from "clsx";

export const styles = {
  skillDiv: clsx( 'flex flex-col items-center justify-center' ),
  skillIcon: clsx( 'block text-2xl text-secondary text-center dark:text-white' ),
  skillText: clsx('block text-center text-sm text-secondary dark:text-white'),
};