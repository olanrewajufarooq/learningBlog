import clsx from "clsx";

export const styles = {
  skillDiv: clsx( 'flex flex-col items-center justify-center space-x-2' ),
  skillIcon: clsx( 'block text-2xl text-secondary text-center' ),
  skillText: clsx('block text-center text-sm'),
};